[project]
name = "noto"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard]>=0.141.1",
    "sudachidict-core>=20260723",
    "sudachipy>=0.6.11",
]
[tool.fastapi]
entrypoint = "main:app"
[tool.setuptools.packages.find]
where = ["."]
